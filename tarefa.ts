const multiplicacao = (num1: number, num2: number): number =>{
    return num1 * num2
}

console.log(multiplicacao(1, 3))

const printNome = (nome: string): string =>{
    return 'Olá '+ nome + '!'
}

console.log(printNome('Lucas'))